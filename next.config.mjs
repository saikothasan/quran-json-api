/** @type {import('next').NextConfig} */
const nextConfig = {
  // Set the output to static HTML export
  output: 'export',

  // Optionally specify other configurations, such as basePath, trailingSlash, etc.
  // basePath: '/my-app',
  // trailingSlash: true,
};

export default nextConfig;
